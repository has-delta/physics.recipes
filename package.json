{
  "name": "physics.recipes",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "parcel index.html",
    "prebuild": "rm -rf dist/",
    "build": "npm run prebuild && parcel build --no-source-maps index.html",
    "deploy": "npm run build && ./node_modules/.bin/gh-pages -d dist/"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "gh-pages": "^2.1.1",
    "parcel-bundler": "^1.12.3",
    "sass": "^1.22.10"
  },
  "dependencies": {}
}
